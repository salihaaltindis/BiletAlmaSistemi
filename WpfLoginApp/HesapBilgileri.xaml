<UserControl x:Class="WpfLoginApp.HesapBilgileri"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfLoginApp"
             mc:Ignorable="d" 
             FontSize="15"
             Foreground="White"
             FontWeight="Bold"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Margin="30">
        <StackPanel>
            <Label Content="User Name" Foreground="White"/>
            <TextBox x:Name="usernameTextBox" ></TextBox>

            <Label Content="Name" Foreground="White"/>
            <TextBox x:Name="nameTextBox"></TextBox>

            <Label Content="Old Password" Foreground="White"/>
            <PasswordBox x:Name="oldPasswordBox"></PasswordBox>

            <Label Content="New Password" Foreground="White"/>
            <PasswordBox x:Name="newPasswordBox"></PasswordBox>

            <Button Margin="100 20 " Click="Button_Click">Değiştir </Button>
        </StackPanel>
    </Border>
</UserControl>
